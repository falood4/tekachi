import 'package:flutter/material.dart';

class AptitudeTestHistory extends StatelessWidget {
  const AptitudeTestHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Aptitude Test History')),
      body: const Center(child: Text('This is the Aptitude Test History Page')),
    );
  }
}
