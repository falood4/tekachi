import 'package:flutter/material.dart';

class Hrtesthistory extends StatelessWidget {
  const Hrtesthistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HR Test History')),
      body: const Center(child: Text('This is the HR Test History Page')),
    );
  }
}
