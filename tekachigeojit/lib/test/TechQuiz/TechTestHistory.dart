import 'package:flutter/material.dart';

class TechnicalTestHistory extends StatelessWidget {
  const TechnicalTestHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Technical Test History')),
      body: const Center(child: Text('This is the Technical Test History Page')),
    );
  }
}
